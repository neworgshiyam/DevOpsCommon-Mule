name:  Mule_Build
on:
  workflow_call:
   inputs:
     Repository:
          type: string
  workflow_dispatch:
jobs: 
  Build-Validation:
      runs-on: ubuntu-latest
      steps: 
        - name: CheckOut SourceCode
          uses: actions/checkout@v3
          with:
            repository: ${{ inputs.Repository }}
            
        - run: |
                cd ./ 
                ls -lart
