name: Mulesoft - Deploy
on:
  workflow_call:
   
jobs:
      
   DeploytoMule:
  
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v3
        
        - uses: actions/setup-java@v3
          with:
              java-version: '11'
              distribution: 'adopt'
              
        - name: Version Number
          run: echo "REL_VERSION = ${{ inputs.version }}"
              
        - name: Deploy to Sandbox        
          run: echo "Deploy to Sandbow"
